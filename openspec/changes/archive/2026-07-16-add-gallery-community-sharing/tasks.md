# Gallery community sharing implementation tasks

## 1. Checked contracts and persistence foundations

- [x] 1.1 Complete the checked wire contract before implementing handlers: define every owner, public discovery, Untrusted-content, Download, Save-a-copy operation recovery, report, and Administrator path with its method, authentication boundary, request and response schema, status codes, response headers, public-response precedence, idempotency and listing or profile revision semantics, plus reusable OpenAPI components for Gallery identifiers, states, optional current and historical permission evidence, effective-access restrictions, Creator profiles, Appeal policy and deadline evidence, cursor pages, operation outcomes, and stable errors.
- [x] 1.2 Define and generate checked language-neutral cover, safety, and copy job contracts with contract version, immutable input snapshot, state and terminal result enums, API and Worker field ownership, fenced lease and attempt semantics, object-layout revision, stable failures, quota and source-retention effects, additive compatibility rules, and N/N-1 TypeScript and Rust fixtures before job persistence or runtime implementation.
- [x] 1.3 Add additive migrations for one Creator profile per User with a profile concurrency revision, Gallery listings, immutable current revisions, non-public update proposals, grant acceptances, opaque slugs, listing concurrency revisions, covers, listing-scoped engagement aggregates, nullable current closure reason, immutable lifecycle events, and non-public closure tombstones, including one-open-listing constraints and the six checked closure reasons.
- [x] 1.4 Add additive migrations for scoped Administrator authority, Gallery reports, immutable case snapshots, governance evidence and object-retention holds, versioned Gallery Appeal policies, governance decisions with snapshotted Appeal policy version and deadline, source-decision-linked derived Public-sharing restrictions, Artifact takedowns, idempotent Appeals including moot resolution, Featured positions, durable in-product notifications, and auditable actor and transition evidence.
- [x] 1.5 Add the missing Artifact and storage quota foundations plus additive migrations for Save-a-copy jobs, per-account copy-rate evidence, quota reservations, durable source-retention references, cross-instance bounded active-download source-read leases, Server-originated copy source kind, immediate-source, root-source, and root-Creator provenance, privacy-retention deadlines, and reconciliation leases.
- [x] 1.6 Extend the Drizzle schema and application domain types with current and proposed Gallery revisions, independent listing and review state, effective public access, copy and governance records, and typed transition results without merging Publication and Gallery projections.
- [x] 1.7 Add deep Gallery repository interfaces and PostgreSQL adapters for listing management, proposals, profiles, grants, discovery, covers, engagement aggregates, governance, copy jobs, provenance, notifications, copy and Download source liveness, and retention instead of expanding the existing Publication modules.
- [x] 1.8 Add database and repository tests for open-listing and restoration uniqueness, profile-per-User uniqueness and first-share revision races, lifecycle first-commit-wins and the deletion-conversion exception, closure-reason transition rules, never-public and withdrawn slug retirement, proposal compare-and-swap, current-grant absence and historical evidence, governance history and derived-basis reversal, quota reservations, copy and Download source-retention liveness, copy-rate evidence, aggregate preservation and reset, provenance, and the absence of cross-User Content-bundle references.
- [x] 1.9 Add migration tests proving existing Artifacts, Publications, Share links, and Users receive no listing, profile, grant, aggregate, or public side effect during rollout.
- [x] 1.10 Define the versioned Artifact and storage quota policy source, safe default limits, controlled change process, and authoritative usage, reservation, commit, release, and reconciliation accounting used by Save a copy.

## 2. Deployment eligibility and untrusted-content security

- [x] 2.1 Add typed Gallery configuration that defaults to disabled and declares trusted Web and API addresses, the Untrusted-content origin and registrable site, credential and cookie scope, network policy, grant revision, approved Gallery Appeal policy, challenge verifier, Administrator authority, reporting, notification, Appeal, governance, isolated-content, and live-readiness inputs.
- [x] 2.2 Implement one fail-closed eligibility policy and shared runtime gate consumed by API startup, deployment validation, public request admission, and new expanding operations, covering same Origin, another port, sibling subdomains, shared registrable sites, credential-spanning cookies, configuration or live-capability loss, recovery without redeploy, and an eligible isolated topology.
- [x] 2.3 Add an isolated local test overlay and public-production ingress for the Untrusted-content site while keeping current shared-host Compose, intranet, and shared-test profiles Gallery-ineligible with no force override.
- [x] 2.4 Add an independent content-only Hono application entrypoint, dependency graph, listener, deployment, and ingress limited to route-disjoint public-player and Administrator-review credential validation, public effective-access or case-bound candidate lookup, manifest reads, private-object streaming, policy headers, health, and telemetry, with no Better Auth, management `buildApp`, management routes, aggregate writer or mutation adapter, ambient credentials, credentialed CORS, object keys, signed storage URLs, or public slugs in logs; keep it fail-closed until task 3.8 supplies both credential paths.
- [x] 2.5 Apply `no-store` and `no-referrer`, document and asset response policy, iframe `sandbox="allow-scripts"` without `allow-same-origin` or child Fullscreen API authority, trusted-parent Full screen, no content cookies or persistent storage, direct-navigation CSP sandbox restrictions, framing and indexing controls, and CORS without credentials only for bound manifest files while blocking every external network and browser capability outside the contract.
- [x] 2.6 Add the checked initial `GallerySafetyPolicy` with content limits, stable finding and reason codes, pass, reject, or review mapping, evidence digest and replay rules, plus deterministic self-contained-content inspection for known external dependencies; keep the runtime network boundary authoritative for dynamic requests that inspection cannot prove absent.
- [x] 2.7 Add content-runtime security fixtures with relative classic and module scripts and relative data fetches plus tests for separate-site credential isolation, sibling-subdomain rejection, direct top-level entry, opaque Origin, cache reuse, referrer suppression, no `Set-Cookie`, storage and Service Worker denial, response policy, remote and programmatic request blocking, child Full-screen denial, trusted-parent controls, route-disjoint Administrator-review credentials, role revocation and case or proposal closure, and fail-closed startup and runtime capability loss.
- [x] 2.8 Redact dynamic Gallery path segments, player credentials, and Administrator-review credentials through stable route templates in API and content-runtime access, error, trace, and header logging before any public opaque slug or bound URL segment can reach the structured log record.

## 3. Owner listing, profile, grant, and safety application

- [x] 3.1 Implement the Gallery aggregate transitions through one listing-revision serialization boundary for initial Pending state, Listed current revision, non-public update proposal, Withdrawn with `creator_withdrawal`, `artifact_deleted`, or `account_deleted`, Removed with `initial_policy_rejection`, `initial_governance_block`, or `administrator_removal`, Administrator Removal only from lifecycle Listed with a committed revision, ordinary competing closures as first-commit-wins, the explicit Artifact/account-deletion conversion exception, and restoration only for a previously public Administrator Removal with one-open-listing enforcement.
- [x] 3.2 Implement proposal creation and compare-and-swap promotion so a Listed current revision stays committed during checks or review and serves only while eligible, pass promotes the complete candidate atomically only when no governance block applies, initial rejection or governance block closes the never-public listing and slug only from its expected revision, update rejection preserves the current revision, and every closure or governance block fences later asynchronous promotion or terminal overwrite.
- [x] 3.3 Implement the optional current Gallery permission-grant read model and exact versioned historical acceptance evidence, including one indivisible View, Gallery Download, and Save-a-copy permission bundle with rejection of per-listing narrowing or a Creator-selected license, stable no-current-grant unavailability without fabricated terms, the canonical Agent permission action only when current evidence exists, renewed acceptance for a Share or Update proposal when policy requires it, and explicit exemption for view, withdrawal, and accepted-operation recovery.
- [x] 3.4 Implement authenticated Creator-profile staging and GET/PATCH operations that require explicit first-share display-name confirmation, never derive a public name from email, accept optional bounded biography and platform-managed safe raster avatar, enforce one profile per User and owner-only revision-checked writes, serialize concurrent first-share create or equivalent reuse before proposal acceptance, reject conflicting fields, and expose the profile publicly only after the first listing promotion.
- [x] 3.5 Add Version-specific Gallery cover records and an enqueue interface used during accepted share or update orchestration without making cover completion a listing prerequisite.
- [x] 3.6 Add a recoverable Gallery safety Worker job using the task 1.2 contract and snapshotted `GallerySafetyPolicy`; inspect one committed candidate, return versioned findings and pass, reject, or review evidence, prevent stale or incompatible results from committing, and let the API own all policy transitions; wire fenced claim, lease renewal, heartbeat, shutdown, readiness, and failed-attempt recovery into `worker/src/main.rs`.
- [x] 3.7 Implement owner-authorized listing view, share, update, and withdraw operations that validate current grant availability and required permission acceptance before staging or mutating a Creator profile, listing, proposal, or grant evidence, then orchestrate profile confirmation and revision, ready-Version ownership, metadata validation, safety proposals, governance eligibility, cover enqueue, idempotency, expected listing revision and terminal-transition conflicts, irreversible replacement confirmation after reversed `administrator_removal`, and honest durable outcomes; accepted same-key replay is read-only operation recovery that does not enter or rerun new-admission gates and cannot create a new mutation.
- [x] 3.8 In the trusted API, atomically authorize and issue short-lived opaque public player credentials bound to `(listing, committed revision, Version)` and increment one view per new credential, plus separate Administrator-review credentials bound to `(Administrator, case or proposal, candidate Version)` with no public or aggregate effect; require public effective access or active Administrator authority and open reviewable state on every entry and relative asset request, use `no-store` and `no-referrer`, normalize paths, stream only the bound manifest, provide static evidence when isolated review is unavailable, and test public credential replay without another view, delayed assets across update, cache reuse, referrer and log redaction, immediate denial after disablement, closure, restriction, eligibility loss, role revocation, or case closure, review-credential separation, expiry, and traversal.
- [x] 3.9 Keep Publication and Gallery mutations independent and add an account-closure hook that moves Pending, Listed, and previously public `administrator_removal` listings to Withdrawn with `account_deleted`, leaves never-public Removed listings at `404` but permanently non-restorable, closes proposals and listing-removal Appeals as moot, retires URLs and profiles, and preserves active governance evidence, unrelated governing cases, accepted copy snapshots, and bounded Download leases without adding account-deletion UI in this change.
- [x] 3.10 Update Artifact deletion transactions to warn, close proposals and listing-removal Appeals as moot, move Pending, Listed, and previously public `administrator_removal` listings to Withdrawn with `artifact_deleted`, leave never-public Removed listings at `404` but non-restorable, and retire applicable URLs before management-resource removal; retain active governance evidence holds and unrelated governing cases, source objects referenced by accepted copy jobs, and Version objects held by cross-instance bounded Download leases, release each through its owning finish, abort, expiry, or reconciliation path, and leave completed independently owned copies unchanged.
- [x] 3.11 Add owner profile and Gallery HTTP routes that implement the checked contract for grant view, listing state, allowed actions, share, update, withdraw, and profile read and write, including idempotency, proposal, revision, permission, policy, and retired-URL errors.
- [x] 3.12 Add API, Worker-runtime, and contract tests for profile privacy and concurrent first-share staging or edit conflicts, zero profile, listing, proposal, and grant-evidence side effects when current grant evidence or acceptance is absent, indivisible grant enforcement and Creator-license rejection, safe metadata, invalid and historical Versions, one-open-listing enforcement, every closure reason, Remove-versus-Withdraw and Pending-Withdraw-versus-reject or block races, deletion-versus-terminal behavior, initial-governance dead-state prevention, proposal pass, rejection, review, and infrastructure failure, stale promotion, same-key input conflict, Publication independence, permanent withdrawal, allowed and forbidden restoration, new-slug re-share, deletion evidence, policy-version evidence, and safety-job claim, lease, heartbeat, shutdown, readiness, compatibility, and recovery behavior.

## 4. Covers, public discovery, and Creator projection

- [x] 4.1 Extend the isolated thumbnail Worker path through the task 1.2 cover-job contract to produce Gallery covers keyed by fixed Version and renderer revision, then test placeholder, success, terminal failure, retry, compatibility, reuse, lease fencing, and stale-result behavior.
- [x] 4.2 Build a public projection that exposes only eligible approved plain-text listing metadata, current Creator fields, safe static cover or avatar identity, opaque URLs, and deterministic ordering keys without private or governance data.
- [x] 4.3 Implement cursor queries with complete order tuples: Featured position then stable listing ID, and immutable listing creation time descending then stable listing ID for default, Newest, search, exact-tag, and Creator collections.
- [x] 4.4 Implement case-insensitive matching without changing relevance scores, exact tags, complete effective-access filtering, and current Creator display-name updates while excluding Pending, Withdrawn, Removed, Restricted, taken-down, disabled, or otherwise ineligible results.
- [x] 4.5 Implement the anonymous Gallery, listing metadata, and Creator profile adapters declared by task 1.1. Listing-scoped routes use the exact eligible `200`, non-public `404`, Withdrawn `410`, and disabled or ineligible pre-lookup `503` matrix. Creator-profile routes return `200` with an empty collection for a previously public profile with no eligible listings, generic `404` for an unknown, staged, or account-deleted profile, and pre-lookup `503` while Gallery is disabled or ineligible; they never use listing-specific `410` or expose emails, account identifiers, private Artifact names, object keys, raw evidence, or closure reasons.
- [x] 4.6 Apply trusted-page indexing only after reporting, takedown, and administration are operational, and keep every Untrusted-content response excluded from indexing with canonical links only to trusted listing pages.
- [x] 4.7 Add discovery, profile, and cover tests for deterministic cursors under insertion, immutable card creation time, search and tags, plain-text escaping, remote-avatar rejection, static cards, placeholder behavior, per-User profile uniqueness, equivalent and conflicting first-share races, revision-checked profile edits, account closure, durable Featured removal with no automatic return, URL opacity, every listing response, public empty-profile `200`, unknown, staged, and account-deleted profile `404`, disabled or ineligible profile `503`, and the prohibition on profile `410`.

## 5. Gallery Download and Save a copy

- [x] 5.1 Adapt normalized ZIP assembly to accept a Gallery authorization bound to one fixed committed Version and emit exactly its manifest files, with source and permission metadata outside the ZIP; atomically acquire a durable cross-instance source-read lease, enforce instance and deployment concurrency ceilings, bounded buffers and streaming back-pressure, abort at maximum duration, and perform exactly-once finish, abort, and expiry cleanup.
- [x] 5.2 Add the anonymous Gallery Download endpoint with dedicated source rate limits, no Creator product-quota charge, and one atomic start-time boundary for effective-access authorization, Version binding, lease acquisition, accepted-download aggregate recording, and stream acceptance that lets the stream finish within its maximum duration while disablement, closure, governance, Artifact deletion, or account deletion blocks later requests.
- [x] 5.3 Implement authoritative per-Copier account rate limiting plus Artifact and storage quota reservation before accepting a new Save a copy, without charging the source Creator.
- [x] 5.4 Implement one serialized new or unknown-key Save-a-copy acceptance that atomically verifies a publicly eligible Listed current revision, binds its immutable listing revision and Version snapshot, consumes the rate-limit unit, reserves quota, acquires one durable source-retention reference, and creates the job before returning acceptance; serialize that boundary against source deletion, accept an editable default title, handle normalized-input idempotency conflict and intentional duplicates, and return accepted, processing, ready, failed, cancelled, or indeterminate quota-aware outcomes without an inaccessible-source oracle; authenticated same-key recovery returns the already accepted operation without new source authorization.
- [x] 5.5 Add Worker execution for the Server-originated Gallery copy source kind through the task 1.2 contract; create a complete Artifact, ready Version, and Copier-owned Content bundle without exposing or reusing a source User's bundle across ownership boundaries, and wire fenced claim, lease renewal, heartbeat, graceful shutdown, readiness, compatibility rejection, and failed-attempt recovery into `worker/src/main.rs`.
- [x] 5.6 Persist immutable immediate-listing and fixed-Version evidence plus root-listing, root-Version, and root-Creator provenance across copy generations; preserve it through ordinary Artifact management and later Gallery sharing while keeping lineage identifiers out of public responses.
- [x] 5.7 Add copy-job reconciliation, bounded concurrency, abandoned-object cleanup, ready quota commit, failed or cancelled quota release, indeterminate reservation and source-reference holding, exactly-once source-reference release with last-reference cleanup, and retry handling that never exposes a partial ready Version, double-counts rate limits, or creates duplicate resources for one idempotency key.
- [x] 5.8 Implement the accepted-copy lifecycle matrix: continue the fixed snapshot after source Owner update, withdrawal, Artifact deletion, or distinct source-account deletion; derive unavailable attribution only when the deleted account is the preserved root Creator; give Copier-account deletion precedence for self-copy; hold one durable source reference per job until terminal; serialize ready commit against Administrator Removal, takedown, restriction, Gallery disablement, deployment ineligibility, and Copier-account deletion; cancel and release when a block or Copier deletion wins; include a ready copy in account cleanup when ready wins; and propagate content governance to a ready provenance-matching copy without including equivalent independent Uploads.
- [x] 5.9 Implement the Download, Save-a-copy acceptance, and authenticated accepted-operation recovery adapters declared by task 1.1 and add API, Worker-runtime, storage, and concurrency tests for ZIP bounds and aborts, durable Download lease acquisition, expiry and release, Artifact and account deletion races, contents, closure races, response precedence, rate limits, quota policy and contention, atomic Save-a-copy acceptance versus source deletion, same-key recovery after closure or disablement, idempotent conflict, intentional duplicates, self-copy deletion, root versus non-root source-Creator deletion across provenance generations, cross-User isolation, provenance chains, multiple source references, duplicate terminal delivery, last-reference cleanup, every source-event race, claim and lease behavior, graceful shutdown, readiness, compatibility, cancellation, and failure reconciliation.

## 6. Governance, administration, notifications, and privacy

- [x] 6.1 Add explicit scoped Administrator role assignment and authorization guards for queue reads, decisions, Appeals, and Featured changes, with audited use and no Artifact ownership authority.
- [x] 6.2 Implement the anonymous challenge-verifier interface with one configured production adapter and deterministic test adapter, and make Gallery readiness fail when production verification is unavailable.
- [x] 6.3 Add signed-in and anonymous Gallery report submission that atomically accepts only a publicly accessible Listed current revision, binds its listing revision and fixed Version, creates the minimum immutable evidence snapshot and retention hold, enforces approved categories, bounded escaped plain-text detail, challenge verification and dedicated rate limits, and exposes no reporter identity or concurrent-closure existence signal.
- [x] 6.4 Implement the minimal review queue with report, listing, current or proposed Version, Creator, Source attribution, and prior decision evidence while denying and hiding it from unauthorized Users; preview non-public candidates only through a case-bound isolated review credential that revalidates active role and open case or proposal on every request, and fall back to static evidence plus stable preview unavailability.
- [x] 6.5 Implement audited dismissal; Remove from Gallery only for lifecycle-Listed committed listings; restriction; clearance only after every review and restriction basis closes; Artifact takedown; atomic snapshot of the active approved Gallery Appeal policy and Server-time deadline for every appealable decision; Appeal decisions independent from later restoration eligibility; Featured placement and durable ineligibility removal without automatic return; and restoration only for a previously public `administrator_removal` with no replacement.
- [x] 6.6 Enforce Artifact takedown as a Reviewing basis that independently blocks effective access and Artifact-level Public-sharing restrictions as the higher-precedence Restricted projection; preserve Publication and committed listing state, block new or extended public sharing, Copy link, and Gallery proposal submission or promotion, close an update proposal without changing its committed revision, close a never-public initial listing as `initial_governance_block`, permit read-only management, Stop sharing link, and Gallery withdrawal, and after each basis clears recompute status and resume both a still-scheduled Publication and an existing Listed committed revision only when no block remains without recreating a closed lifecycle resource.
- [x] 6.7 Add privileged immutable-provenance matching for serious takedowns that opens review bases and applies source-decision-linked Artifact restrictions to only direct and descendant Gallery-saved copies, projects any affected listing as Restricted, excludes equivalent independent Uploads, keeps later Version review explicit, exposes no content-existence oracle or private-copy deletion, and on reversal closes only matching derived bases, resolves their Appeals, and recomputes copies without clearing unrelated governance.
- [x] 6.8 Implement Creator Appeals for active listing Removal and Artifact-Owner Appeals for active takedown or Public-sharing restriction, including private copy Owners without profiles; atomically enforce affected-party, in-force decision, snapshotted policy and deadline, duplicate, idempotent-retry, and reversal-race rules; preserve existing deadlines across policy changes; make a later direct reversal atomically resolve an accepted Appeal as reversed or moot and recompute dependent access, restoration, and replacement blocks; render Appeal explanations, administrative rationales, and durable notification text as bounded escaped plain text with category, rule, effect, Appeal path, and deadline when appealable but no reporter identity, markup execution, automatic links, remote loads, or per-engagement notifications.
- [x] 6.9 Record a non-identifying listing view atomically in the trusted API when it issues a new player authorization, a Download at authorization, and a copy at ready commit; never count content-runtime entry, credential replay, or asset requests, preserve aggregates through update promotion, reset them for a new listing, and exclude them from every public response and ranking query.
- [x] 6.10 Add retention reconciliation that deletes or irreversibly aggregates raw Viewer signals within 30 days and releases governance evidence and object holds only after the case and every accepted Appeal are terminal, every applicable snapshotted Appeal deadline has passed, the approved governance-retention deadline has passed, and no other legal or job reference remains; implement the no-Appeal-deadline branch for non-appealable terminal cases and keep holds when an accepted Appeal outlives its submission deadline; delete unreferenced evidence objects and never create a public Viewer identity or permanent browsing history.
- [x] 6.11 Implement the governance adapters declared by task 1.1 and add authorization, challenge, transition, non-appealable initial-rejection result, restriction projection, restoration versus confirmed-replacement race, report-acceptance race, takedown propagation and reversal, Appeal-policy snapshot and policy-change stability, both orderings of Appeal acceptance versus decision reversal including terminal resolution after acceptance, duplicate and late Appeal, derived-basis Appeal resolution, non-appealable and long-running-Appeal retention branches, safe-text rendering, audit, notification, Featured, aggregate, and retention contract tests for every operation.
- [x] 6.12 Mount every new trusted owner, public discovery, copy, report, and Administrator route group through `api/src/http/app.ts` and `AppDependencies`; deploy the content-only application independently and add dependency-graph and route-boundary tests proving it cannot inherit management authentication, credentialed CORS, mutation adapters, or management-only middleware and that public-player and review credentials authorize only their disjoint read routes.

## 7. Public Gallery Web experience

- [x] 7.1 Add typed Web API clients for public collections, trusted listing metadata, Creator profiles, Download, Save a copy, and Report with explicit signed-out, signed-in, unavailable, restricted, processing, and action-required outcomes.
- [x] 7.2 Add `/gallery`, `/gallery/{opaqueSlug}`, and `/creators/{opaqueSlug}` routes and public navigation before the management authentication gate, keeping `/artifacts` authenticated and showing the explicit unsupported experience on mobile and tablet.
- [x] 7.3 Build static Gallery cards and cursor-driven Featured, Newest, search, tag, and Creator collections with the specified order tuples, without executing Artifact content or displaying engagement counters.
- [x] 7.4 Build the trusted listing page with escaped plain-text metadata, platform-managed raster avatars and covers, provenance, and trusted View, Full screen, Save a copy, Download, and Report controls outside the isolated cross-Origin iframe.
- [x] 7.5 Implement sign-in handoff and asynchronous status handling for Save a copy, normalized Download initiation, and signed-in or challenged anonymous reports without allowing Artifact content or cross-frame messages to invoke platform actions.
- [x] 7.6 Apply governance-gated indexing to trusted pages, preserve `noindex` on Artifact content, and emit canonical discovery links only to trusted listing URLs.
- [x] 7.7 Add component and isolated-topology Playwright coverage for public navigation, placeholders, stable cursors, Creator profiles, metadata injection, listing proposal promotion, trusted-parent Full screen with child denial, signed-out Download, copy sign-in and completion or cancellation, reports, ordered non-disclosing `404`, `410`, and pre-lookup `503` pages, availability-before-device precedence, indexing gates, and unsupported devices.

## 8. Owner and administrator Web experience

- [x] 8.1 Rename Web link controls and lifecycle statuses while preserving explicit Share with link for another ready Version when Link active and for reactivation when Expired or Link stopped, including existing batch behavior.
- [x] 8.2 Project an independent `Restricted` notice beside the underlying Link sharing and Gallery states, disable Copy link and public-expanding changes, and retain read-only management plus Stop sharing link.
- [x] 8.3 Present separate Share with link and Share to Gallery controls on eligible Artifact cards and detail pages and separate Manage link and Manage Gallery paths when either channel exists, never a generic Share action.
- [x] 8.4 Add first-share Creator display-name confirmation and authenticated Creator profile editing for safe avatar, biography, and display name without exposing or deriving email.
- [x] 8.5 Build Share to Gallery and Manage Gallery flows for Version selection, metadata, current or policy-renewed grant acceptance, explicit no-current-grant unavailability, proposal status, revision conflicts, cover placeholder, atomic promotion result, and irreversible withdrawal; show only state-valid actions for Pending and Listed, offer a fresh share after eligible Withdrawn closure, corrected `initial_policy_rejection`, and cleared or reversed `initial_governance_block`, show governing state while a Removed decision or Appeal remains active, and require a destructive warning and confirmation before replacement permanently forfeits a reversed `administrator_removal` predecessor.
- [x] 8.6 Update single and batch Delete confirmation and outcomes to warn about proposal, listing, and URL retirement plus delayed physical cleanup for active governance, accepted-copy, or already authorized Download holds, while preserving batch Share with link and excluding batch Export and batch Share to Gallery; test each warning and outcome branch.
- [x] 8.7 Build the minimal Administrator queue, decision, Appeal, Featured, Creator-notification, and affected-Owner notification inbox surfaces with explicit authorization failures, bounded escaped text, and durable outcomes.
- [x] 8.8 Add Web unit and isolated-topology end-to-end tests for link re-share and reactivation, restrictions, separate channel controls, profile privacy, grant actions, proposal review, stale revisions, cover failure, withdrawal, deletion, governance, notifications, and no cross-channel side effects.

## 9. CLI, Agent protocol, and official Skill

- [x] 9.1 Extend CLI models and the HTTP client for current and proposed Gallery revisions, profile requirements and revision conflicts, optional current grant revision and exact text with an optional digest, historical accepted-grant evidence, stable no-current-grant unavailability, effective-access status and stable blocking category, current terminal closure reason, allowed actions, and evidenced errors.
- [x] 9.2 Add human `artifact gallery view`, `artifact gallery share`, `artifact gallery update`, and `artifact gallery withdraw` commands and operational help for deterministic defaults, first-share display name, no-current-grant state, idempotency, Version-changing and policy-renewed grant acceptance, eligible Removed-history sharing, irreversible replacement after reversed Administrator Removal, revisions, proposal outcomes, and irreversible withdrawal.
- [x] 9.3 Add the four Gallery operation IDs and canonical `accept_permission` action to the Agent parser, schema, and offline capability discovery while keeping permission acceptance distinct from irreversible confirmation.
- [x] 9.4 Extend Agent envelopes, fixtures, and result mapping so an unlisted Gallery view returns configured current grant evidence or stable no-current-grant unavailability plus history, and Gallery resources preserve effective-access, blocking-category, and terminal closure-reason evidence while mutations include only confirmed listing or proposal resources without inferring Publication effects.
- [x] 9.5 Add CLI parser, HTTP, command, capability-parity, schema, current-grant absence, permission-action including metadata-only renewal, accepted-operation recovery during unavailability, irreversible replacement after reversed removal, Delete evidence for active and restorable-Removed listing retirement, idempotency, confirmation, compatibility, proposal, and indeterminate-evidence tests for all four owner operations.
- [x] 9.6 Update the official Skill to distinguish Share with link from Share to Gallery, resolve deterministic defaults, collect first-share profile data, present exact grant evidence or no-current-grant unavailability, require permission acceptance, irreversible replacement confirmation after reversed Administrator Removal, and withdrawal confirmation, negotiate capabilities, and use only Agent-mode CLI operations.
- [x] 9.7 Extend fake CLI behavior, trigger and behavior evaluations, snapshots, and safety assertions for ambiguous Share wording, Version choice, profile requirements, grant absence and acceptance including metadata-only renewal, eligible Removed-history sharing, irreversible replacement, proposal review, revision conflict, indeterminate results, withdrawal, and unsupported capabilities.
- [x] 9.8 Add a deterministic Skill packaging command that rebuilds `skill/package/shareslices.skill` from reviewed source, fails when the package drifts, and runs as part of the durable `skill:test` or `mise run check` quality gate.
- [x] 9.9 Rebuild and validate the packaged Skill and verify that it never falls back to REST, human output, selected-field JSON, or an unadvertised public Gallery operation.

## 10. Observability, rollout, documentation, and final verification

- [x] 10.1 Extend the executable structured-log schema with stable listing, proposal, copy-job, report, decision, request, attempt, and trace identifiers while redacting route slugs, player and Administrator-review credentials and bound URL segments, raw content, credentials, reporter secrets, request headers, and raw exception evidence.
- [x] 10.2 Add bounded operational metrics, live readiness, and reconciliation for safety proposals, covers, copy jobs, reports, notifications, retention work, and configuration or runtime eligibility failures without substituting those metrics for product aggregates or using them for ranking.
- [x] 10.3 Add rollout tests proving Gallery stays disabled by default, reads verifiable current terms and live capability readiness instead of a force flag, fails closed on an unsafe site boundary or configuration or runtime governance-capability loss, transitions every public entrypoint to pre-lookup `503` without redeploy, always keeps authenticated view and permanent withdrawal available, does not disable administration, notifications, or Appeals merely because public Gallery is off but returns stable unavailability when their own dependencies fail, blocks every public or expanding operation, and resumes without lifecycle mutation only after every gate is restored.
- [x] 10.4 Implement and test a rollback coordinator that leaves existing resources unlisted and unchanged; disablement fences new claims, player access, and public lookup, pauses or requeues cover and safety jobs, cancels or reconciles non-terminal copies, cleans attempt objects, releases quota and copy source references or holds indeterminate ones correctly, preserves already authorized Download leases until finish, abort, or maximum-duration expiry, resumes deleted-source cleanup after the last hold, rejects stale Worker commits, and retains governance data, completed copies, and completed Downloads.
- [x] 10.5 Add full-stack isolated-topology scenarios for independent link and Gallery sharing, candidate update approval and rejection, non-appealable initial rejection, every terminal-transition race, permanent withdrawal and new-slug re-share, eligible fresh sharing after every Removed history, confirmed replacement versus restoration, anonymous discovery and Download, duplicate and rate-limited copies, accepted-operation recovery after closure or disablement, every accepted-copy source, self-copy, governance, eligibility, Copier-deletion and Download-deletion races, source-object retention, reports, direct-takedown review projection, restrictions, propagated-restriction reversal, Appeal acceptance, duplication, lateness, and reversal races, and Artifact takedown.
- [x] 10.6 Add browser security scenarios for registrable-site and credential isolation, separate public-player and case-bound Administrator-review credentials, review denial after role revocation or case closure, revision-scoped delayed assets across update, relative classic and module scripts and data fetches, opaque Origin, direct content navigation, `no-store`, `no-referrer`, cache reuse denial, credential log redaction, no cookies or persistent storage or Service Worker, sandbox and Permissions Policy behavior, Artifact-initiated Full-screen denial, blocked external networking and untrusted platform-action attempts, manifest-only assets, metadata escaping, safe avatars, and trusted-parent Full screen controls.
- [x] 10.7 Verify every task 1.1 owner, public, content, copy, report, and administration contract plus language-neutral error and job-schema behavior against the running stack rather than authoring or completing contracts at the end.
- [x] 10.8 Synchronize `api/openapi/openapi.yaml`, `docs/design/modules.md`, deployment documentation, CLI help, Skill documentation, and durable product terminology with the implemented contract without depending on the active change directory.
- [x] 10.9 Run focused API, Web, Worker, CLI, Skill, deployment, OpenAPI, Markdown, spelling, and OpenSpec validation while iterating.
- [x] 10.10 Make `mise run check` and the isolated desktop Gallery end-to-end suite pass before implementation handoff and keep Gallery disabled if any required check or readiness gate fails.
- [x] 10.11 Before enablement, check in the approved exact grant, notice, Appeal, and retention text plus the initial versioned `GalleryAppealPolicy`; configure and test a production `ChallengeVerifier`; document and exercise secure initial Administrator bootstrap and revocation; and approve the initial versioned `GallerySafetyPolicy` with fixtures and rollout evidence.
