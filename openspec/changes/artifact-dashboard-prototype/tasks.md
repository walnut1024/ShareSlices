# Artifact Dashboard Prototype — Tasks

## 1. Product and contracts

- [x] 1.1 Record export, deletion, and Share link expiration policy in `PRODUCT.md`.
- [x] 1.2 Define the OpenSpec delta and implementation design.

## 2. API behavior

- [x] 2.1 Add expiration, export, and delete application/repository behavior with failing tests first.
- [x] 2.2 Add authenticated HTTP routes and update the checked OpenAPI contract.

## 3. Web behavior

- [x] 3.1 Add client methods and regression tests for the expanded actions.
- [x] 3.2 Rebuild the Artifact dashboard and dialogs from the prototype with shadcn Base UI.

## 4. Verification

- [x] 4.1 Run focused tests, typechecks, and builds.
- [x] 4.2 Verify the dashboard at 1440×900 and run `mise run check`.
