# Kubernetes and Cloudflare Deployment Targets Tasks

<!-- cspell:words Hyperdrive secretless -->

## Execution Dependencies

The numbered sections group work by responsibility; they are not permission to ignore these gates:

- Complete and archive `consolidate-local-development-stack` before moving Compose files or ownership.
- Task 2.11 may create the non-Compose module skeleton before that dependency, but only 13.1-13.3 move Compose inputs, controller policy, or ownership. Execute 13.1-13.3 immediately after the dependency and before either production target relies on the local harness.
- Complete the provider-neutral and Node-side runtime seams in 4.1-4.3,
  4.5-4.7, 4.9, and 7.1-7.4 before 4.10 and 13.4-13.10 update and
  accept the Compose role graph. Cloudflare-only entrypoints and parity work in
  4.4 and 4.8, and Cloudflare wake/Container work in 7.5-7.10 and 7.13, do not
  block Compose or Kubernetes. Task 7.11's Kubernetes resident-mode behavior
  must pass before Kubernetes processing is accepted; its Cloudflare thumbnail
  behavior remains independently gated by 1.8 and the Cloudflare tasks.
- Complete the shared lifecycle and release contracts in sections 2, 3, and 8 before target lifecycle work in sections 10 and 12.
- After those shared gates, Kubernetes composition/lifecycle work in sections 9-10 and Cloudflare composition/lifecycle work in sections 11-12 may proceed independently. Container-, Resend-domain-, or rollout-dependent Cloudflare work must retain its corresponding section 1 gate and cannot be counted as qualified from a Free-account or `resend.dev` prototype.
- Complete cross-target verification and durable operator documentation in sections 14-15 only after the selected target paths and the canonical Compose harness they rely on are implemented.
- A failed Cloudflare feasibility gate blocks Cloudflare capability completion and final archive, but it does not block Kubernetes implementation. If Kubernetes must ship first, move the unimplemented Cloudflare capability into a follow-up change rather than archiving it as implemented.
- While prototype execution uses Workers Free, tasks 1.7 and 1.8 and every
  Container-dependent assertion in 1.10 remain pending rather than blocking
  independent Free-compatible prototypes. Task 1.7 gates trusted background
  processing; task 1.8 separately gates thumbnail generation and may be deferred
  without hiding the unavailable capability. This sequencing does not waive
  either gate and does not permit the Cloudflare target to be marked complete.
- Treat Workers Free plus the separately enabled R2 subscription only as a
  prototype execution profile. It is not a third target or a production input;
  production `render`, `plan`, and `apply` must reject it. After every live
  prototype, remove or disable all public or continuously invocable resources,
  re-inventory actual provider state, and record an owner and expiry for any
  private prerequisite deliberately retained for the next bounded prototype.
- While no operator-owned sending domain exists, the Resend prototype may use `resend.dev` simulation addresses for API-shape, error, quota-header, idempotency, replay-cutoff, namespace-refusal, and redaction tests. Task 1.9 remains pending until a verified custom domain, disabled-tracking evidence, domain-scoped sending-access credentials, same-team/domain key rotation, and real provider-acceptance evidence pass.
- While no operator-owned Cloudflare zones and distinct registrable sites exist, `workers.dev` may be used only for disposable provider-interface prototypes. Production ingress, trusted/content site isolation, custom-domain activation, and full Cloudflare acceptance remain pending. Buying or creating those external assets is not part of this change.
- Treat separately reported capability readiness as observability, not as a way
  to waive target requirements. A Free-compatible prototype may report
  processing or thumbnail readiness unavailable, but task 1.7 and task 1.8
  still block complete Cloudflare-target qualification.
- Recheck the mutable observations in `evidence/current-prototype-execution-baseline.md` before every live provider prototype. A failed or partial refresh makes the affected inventory `unknown`; do not fall back to a previously observed Supabase project, retained Cloudflare resource, or ambient credential. An authenticated CLI, R2 Paid, an active Supabase Free project, or an available Resend key grants no implicit permission to choose resources, expose public routes, retain billable services, or mark a production gate complete.

## 1. Cloudflare Prototype Feasibility Gates

- [x] 1.1 Pin Wrangler, Workers compatibility date and flags, Container package, and generated Wrangler configuration schema; pin the Cloudflare Terraform provider through an exact `required_providers` constraint and committed multi-platform dependency lock checksums, export and digest its provider schema, and validate every required resource and field against that schema. Record the official-contract baseline used by the release.
- [x] 1.2 Build disposable Worker prototypes for the trusted and content-only Hono graphs and prove request, response, Cookie, error, and streaming semantics against representative existing HTTP contracts rather than accepting bundle success.
- [x] 1.3 Prototype Better Auth, Drizzle, and repository `pg` through the Cloudflare entrypoint without unsupported Node stubs or a Cloudflare-specific account or authorization implementation.
- [x] 1.4 Prototype cache-disabled Hyperdrive for authentication, authorization, Viewer, Gallery, and job-state paths and identify every operation that requires a direct PostgreSQL connection.
- [x] 1.5 Prototype bounded streaming Upload, export, Download, range reads, and private R2 multipart/object operations without buffering complete accepted payloads in Worker isolate memory.
- [x] 1.6 Classify configurable Upload and generated Static Assets limits as provider-observed, release-static, or operator-evidenced; compare the configuration and generated build against the applicable qualified values and reject excesses. Do not describe a dated official table as a live account observation or treat R2 multipart as bypassing one inbound request limit.
- [ ] 1.7 Build a disposable trusted Rust processing Container spike with direct TLS PostgreSQL, bounded execution, least-privilege credentials, qualified whole-Container egress controls, graceful termination, and no browser runtime. This independently gates the Cloudflare target's required background-processing lanes and remains pending while the account lacks Workers Paid.
- [ ] 1.8 Build a separate disposable secretless Rust/Chromium thumbnail Container spike using a read-only browser capture capability plus a distinct controller/output capability through a private execution broker. Record evidence for non-root execution, no host authority, no privilege escalation, capability and system-call restrictions, per-operation egress, audience separation, and termination isolation equivalent to the thumbnail contract. This task may be deferred while Free-compatible prototypes continue, but thumbnail readiness MUST report unavailable and the Cloudflare target MUST remain unqualified until it passes; fail the Cloudflare thumbnail gate if equivalent isolation cannot be proven.
- [ ] 1.9 Prove Resend HTTPS acceptance with required `User-Agent`, verified-domain sending-access key, disabled tracking, logical-delivery idempotency, same-team/domain key rotation, cross-team retry refusal, accepted provider message ID, error-type and quota classification, 24-hour indeterminate handling, and redacted logs.
- [ ] 1.10 In disposable minimal scripts, prove only the provider interfaces needed before implementation: first-script bootstrap constraints; zero-percent App/Content version selection and fetch-Service-Binding version-ID evidence; version-scoped Secret behavior; Jobs `exports` incompatibility with version upload; immediate Jobs deployment identity and Secret preservation; Queue pause/resume and Cron attach/detach control-plane behavior; Container image availability, mixed rollout and retained-image rollback; Worker rollback/version-retention limits; and repeated Terraform/Wrangler ownership operations for Cron, Queue consumers, routes, custom domains, and bindings. Record provisional or blocked results per interface. Integrated nonce/fence, cross-storage quiescence, candidate compensation, full release verification, and end-to-end rollback are implementation acceptance in 11.20, 12.3-12.9, and 15.11 rather than duplicate feasibility prototypes.
- [ ] 1.11 Publish the prototype and field-ownership support matrix; distinguish Workers Free, R2 subscription, Workers Paid, and owned-domain prerequisites instead of collapsing them into one plan label. Record final provider inventory and cleanup evidence, including the owner and expiry of any intentionally retained private prerequisite. A failed gate stops only Cloudflare-target implementation and MUST NOT block Kubernetes delivery or trigger a target-specific product-policy workaround.

## 2. Product Vocabulary and Deployment Contracts

- [x] 2.1 Update `PRODUCT.md` with the mutually exclusive Kubernetes and Cloudflare target boundary, Kubernetes optional-CDN rule, enterprise-SMTP choice, Resend choice, provider acceptance versus inbox delivery, per-delivery provider-namespace pinning, bounded provider idempotency, signed and transactionally serialized manual reconciliation, rollback limits, and the rule that outward Viewer responses remain `no-store` while an implementation may reuse only internal immutable bytes after current authorization.
- [x] 2.2 Add accepted `Deployment target`, `Kubernetes target`, `Cloudflare target`, `Deployment Module`, and target-specific email Adapter terminology to `CONTEXT.md`.
- [x] 2.3 Update `docs/design/modules.md` and the documentation map with the Deployment Module, runtime composition seams, target ownership, and durable fact owners.
- [x] 2.4 Complete and archive `consolidate-local-development-stack` before moving Compose ownership; record the archive-state check as prerequisite evidence for 13.1-13.3, verify this change preserves that implemented contract, and add a `MODIFIED` delta first if implementation discovers any observable local-stack behavior that must change.
- [x] 2.5 Define and validate a versioned discriminated production deployment schema that accepts exactly one of `kubernetes` or `cloudflare` and rejects `compose`, mixed target fields, embedded Secrets, or the wrong email Adapter.
- [x] 2.6 Define and validate the immutable release schema with content digests, qualified provider identities, non-secret Secret revisions, release inventory and ownership markers, provider toolchain baseline, migration checksums, contract revisions, configuration compatibility, and N/N-1 metadata.
- [x] 2.7 Generate the machine-readable route projection for public, Viewer, Preview, Gallery, content-only, health, and forbidden internal route families; require every row to reference an OpenAPI operation or documented route owner and fail projection drift.
- [x] 2.8 Generate the machine-readable cache projection from implemented requirements, including Static Assets header classes, dynamic bypass routes, optional authorization-first full-body Viewer byte caching, complete representation-key inputs, Range/206 bypass, separate internal and outward responses, and private-R2 requirements while preserving every stable dynamic `no-store` response.
- [x] 2.9 Create the shared verification-scenario contract and fixtures for target addresses, required capabilities, disabled capabilities, and stable reason codes, including explicit `not_applicable` results for provider-only checks executed against local Compose.
- [x] 2.10 Add schema and fixture tests proving equivalent valid input is deterministic and missing, mixed, unsafe, or Secret-bearing input fails before target access.
- [x] 2.11 Create the `deploy/contract/`, `deploy/automation/`, `deploy/cloudflare/`, and `deploy/tests/` structure and document their ownership boundaries. Do not move Compose inputs or lifecycle policy in this task; tasks 13.1-13.3 perform that ownership migration after 2.4.

## 3. Deployment Automation Core

- [x] 3.1 Add a repository deployment CLI under `deploy/automation/` and expose it through `mise run deploy -- <command>` with stable JSON results and exit categories.
- [x] 3.2 Define the narrow target Adapter interface used by `doctor`, `render`, `plan`, `apply`, `status`, `verify`, and `rollback` without exposing provider details to lifecycle policy.
- [x] 3.3 Implement common configuration loading, schema-version checks, target selection, target-field rejection, and target-specific prerequisite discovery.
- [x] 3.4 Implement logical Secret references, operator-controlled non-secret revisions, last-responsible-moment resolution, targeted consumer rollout, recursive redaction, and tests that keep values and value-derived fingerprints out of renders, plans, records, errors, and logs; shared signing-key rotation must stage old-plus-new verification, then new signing, then old-key removal after the maximum grant and mixed-runtime lifetime.
- [x] 3.5 Implement canonical release and target-bundle serialization, content-digest verification, qualified provider-identity validation, mutable or reused tag rejection, and deterministic re-render tests.
- [ ] 3.6 Implement `doctor` as a read-only configuration, tool, credential-reference, provider-capability, DNS/TLS, dependency, and compatibility check.
- [ ] 3.7 Implement `render` as a read-only producer of a complete Secret-free target bundle and canonical bundle digest.
- [x] 3.8 Implement `plan` as a read-only observed-state comparison with ordered phases, drift, replacements, security-sensitive changes, destructive-action refusal, an exact plan digest, and the observed-state revision on which the plan depends; a first-installation plan includes the observed absence and expected checksum of the deployment-control schema plus its one permitted bootstrap transition.
- [x] 3.9 Implement an idempotent phase engine for `apply` that requires the authorized plan digest, performs only the plan-authorized control-schema bootstrap when needed, acquires the durable lease, treats its resulting first control revision as the expected transition, re-observes and refuses every other invalidating drift, records confirmed checkpoints, recovers from interruption, never repeats a completed migration, and returns explicit `external_reconciler_required` handoff outcomes.
- [x] 3.10 Implement `status` with desired, handed-off, observed, phase-blocked, partial, failed, indeterminate, verified, drifted, orphaned, and optional-capability states.
- [x] 3.11 Implement the checksum-verified external-PostgreSQL deployment-control schema, deterministic advisory-lock-protected first-install bootstrap with no provider mutation, durable operation lease, heartbeat, monotonically increasing fencing token, authoritative phase journal, ambiguous-bootstrap reconciliation, and stale-writer rejection; then mirror target-specific Secret-free active/previous release records only after the lease exists.
- [x] 3.12 Implement compatibility-aware `rollback` that restores recorded application artifacts, refuses an unsafe schema or job-contract transition, and never runs a down migration.
- [x] 3.13 Add release inventory, ownership-marker, orphan detection, and ordered retirement support that detaches traffic and schedules before removing only positively owned resources outside the rollback window.
- [ ] 3.14 Add lifecycle unit and integration tests proving all read-only commands are non-mutating, repeat apply/rollback operations are idempotent, lost leases stop later writes, stale fences fail, and unowned resources are never deleted. For the Cloudflare R2 mirror, cover concurrent first create, lease loss between read and conditional write, precondition failure, and an indeterminate response; require reconciliation from PostgreSQL and forbid direct retry by the old fence.
- [x] 3.15 Define and validate recoverability evidence for PostgreSQL, object storage, IaC state, release bundles, and deployment journals; implement a durable database/object/recovery-manifest consistency marker and stale, missing, and mismatched-evidence fail-closed tests.

## 4. API and Web Runtime Composition

- [x] 4.1 Split configuration into validated API HTTP, maintenance, content-only, migration, Web bootstrap, and background-processing role schemas.
- [x] 4.2 Extract trusted HTTP and content-only Hono application builders whose business Modules are shared by Node and Cloudflare entrypoints.
- [x] 4.3 Add distinct Node entrypoints for API HTTP, maintenance, content-only serving, and one-shot migration without importing unrelated role configuration.
- [ ] 4.4 Add Cloudflare `fetch`, Queue, and scheduled entrypoints over the shared builders without starting resident loops in request isolates.
- [x] 4.5 Remove authentication-email and reconciliation dispatch loops from API HTTP startup and preserve them through the maintenance composition.
- [x] 4.6 Add a trusted-ingress metadata Adapter that discards forged forwarding headers and normalizes Kubernetes trusted-proxy or Cloudflare runtime metadata.
- [x] 4.7 Replace environment-specific Web build arguments with a deployment-provided public runtime bootstrap that contains no Secrets and is safe to serve per installation.
- [ ] 4.8 Add cross-entrypoint contract tests proving Node and Cloudflare return the same statuses, headers, Cookies, errors, and route behavior for shared scenarios.
- [x] 4.9 Add dependency-graph tests proving the content-only builder has no management routes, Better Auth, credentialed CORS, email, Queue control, or mutation Adapter.
- [x] 4.10 Keep the canonical Compose graph synchronized with the new API HTTP, maintenance/authentication-email, content-only, one-shot migration, resident processing, and Web roles; never recombine role authority only for local convenience.

## 5. PostgreSQL and Object-Storage Adapters

- [x] 5.1 Introduce the minimum database composition seam for direct Node PostgreSQL, cache-disabled Hyperdrive, and direct migration or trusted non-browser processing-Container connections without leaking clients into domain Modules.
- [x] 5.2 Route every advisory-lock, migration, unsupported session-state, and long-lived lease-heartbeat operation through one checked-out verified direct client for the whole logical operation; hold migration exclusion and each per-file transaction on that same client.
- [ ] 5.3 Add Hyperdrive compatibility tests for transactions, freshness, prepared statements used by the codebase, timeouts, connection-budget behavior, and an explicit TLS origin-verification mode; include a wrong-host or untrusted-certificate negative case and never treat encryption alone as authenticated origin identity.
- [x] 5.4 Fail Cloudflare `doctor` when a required direct connection, explicit hostname/certificate verification, or cache-disabled Hyperdrive capability cannot be established instead of weakening database semantics; require `verify-full` or a subsequently qualified equivalent rather than treating Hyperdrive `require`, `pg_stat_ssl`, or encryption alone as hostname-verified origin identity.
- [ ] 5.5 Implement the private R2 Adapter behind the existing object-storage responsibility while preserving keys, manifests, metadata, content types, ranges, retries, and cleanup intents.
- [ ] 5.6 Preserve bounded streaming and multipart behavior across S3 and R2 and add parity tests for Upload, committed content, export, thumbnails, Gallery covers, and cleanup; cover R2 same-key write-rate limits with immutable staging keys or conditional/idempotent finalization rather than concurrent blind writes.
- [ ] 5.7 Add negative tests proving neither target returns public bucket addresses, raw object keys, signed storage URLs, raw Uploads, staging objects, or arbitrary objects.
- [ ] 5.8 Add immutable migration checksums and ordered-ancestry validation while retaining per-file transactions and testing failure after every committed migration prefix.
- [ ] 5.9 Add N/N-1 database and cross-runtime compatibility fixtures for every intermediate migration head and reject releases whose schema range or job/object contract is incomplete.
- [ ] 5.10 Define the private broker HTTP wire contract under `api/openapi/` and project it into `deploy/contract/`; implement claim, heartbeat, private R2 access, output upload, and fenced commit outside the secretless Chromium Container, atomically exchange a single-use browser bootstrap grant for a short-lived HttpOnly/SameSite-strict/path-scoped read session, and keep the distinct controller/output capability outside every Artifact-visible surface.
- [ ] 5.11 Add adversarial broker tests for bootstrap-grant replay, capture-session scope and expiry, capability audience and operation confusion, another Version, attempt, path, redirect, browser scheme, inherited descriptor, stale fence, and direct database or R2 access.
- [ ] 5.12 Define the route-free Jobs release-verification `fetch` contract under the checked private `api/openapi/` ownership model and project it into `deploy/contract/`; bind it only through the verifier Worker's explicit Service Binding and require release/fence/nonce scope. Return and verify the executing Jobs Worker version or deployment, embedded release-bundle identity, ordinary-configuration and `exports` digest, configured image reference, and observed migration head. Make each actual Container instance return its embedded build/release/contract identity plus class, stable slot, provider instance, release, fence, and nonce; map the build identity to the release's image digest/provider reference and require every production-capable slot with no prior-image instance selectable. Bound every synthetic database/broker/Container/R2 exercise to a dedicated release/fence/nonce-owned non-product namespace. Add a PostgreSQL terminal nonce and probe sub-fence checked at every authoritative database commit, nonce-scoped active-invocation leases, bounded cross-storage quiescence before final inventory/cleanup, and a tombstone retained through the observed maximum of Queue retention, send/retry delay and schedule, active-invocation lease, interrupted recovery, and cross-storage side-effect intervals plus safety margin; prove Queue pause is not reported as draining in-flight work and no real business record, public route, production Queue, Cron, or general administration caller can reach or be mutated by the harness.

## 6. Target-Specific Authentication Email

- [ ] 6.1 Extend the shared `account-entry` authentication-email responsibility with optional provider identifiers, a separate stable local message identifier, provider-acceptance classification, bounded provider idempotency, non-idempotent side-effect ambiguity, and a manual-reconciliation outcome. Leave unattempted pending records unbound; on first claim atomically freeze the then-current non-secret Adapter kind, declared provider account/team or relay namespace, sender/domain and endpoint identity, configuration revision, serializer/payload digest, logical idempotency key when supported, and local message identifier. Give every provider call a durable attempt ID, fence, phase, and maximum deadline; persist submitting before an external side-effect boundary and, for finite deduplication, atomically freeze a conservative safe-replay cutoff from the first pre-send time, pinned retention, and safety margin that no retry may extend. Permit retry when durable evidence proves complete submission did not occur, or after the prior deadline and observed quiescence when current time remains before that cutoff and a qualified provider-deduplication contract still covers the same namespace, key, and byte-equivalent payload; otherwise require manual reconciliation. Preserve provider-neutral rendering, encrypted records, leases, circuit breaking, terminal outcomes, and payload deletion.
- [ ] 6.2 Make the Kubernetes maintenance role use only the enterprise-SMTP declared relay namespace, endpoint and configuration revision, TLS policy, sender identity, and scoped Secret keys it requires.
- [ ] 6.3 Add a non-delivery SMTP `doctor` probe for declared relay namespace, endpoint identity, DNS, TLS, authentication capability, credential reference, and sender syntax with fully redacted diagnostics; prove envelope acceptance and the server's final successful response after the complete `DATA` transaction only through an explicitly authorized deep test send, and never record `sent` or `provider_accepted` from `MAIL FROM` or `RCPT TO` acceptance alone. Keep one stable message identifier per logical delivery; retry only a fenced attempt durably proven not to have completed submission through the frozen relay/sender identity; and route a crash, lease loss, or timeout during/after `DATA`, lost final response, late completion, or otherwise unknown side-effect phase to manual reconciliation without automatic resend.
- [x] 6.4 Implement the minimal Cloudflare Resend HTTPS contract with a declared non-secret team namespace and transport revision, plus a domain-scoped sending-access API key; `Authorization`, `Content-Type`, stable `User-Agent`, and `Idempotency-Key` headers; required JSON `from`, one-element `to`, and `subject`; selected `text` and `html`; no attachment; and no full-access administrative dependency.
- [ ] 6.5 Derive one provider idempotency key from the immutable logical delivery ID and payload digest; on the first possible send atomically freeze `provider_safe_replay_until` from the local pre-send time, Resend's pinned 24-hour retention, and a clock/transport safety margin. Reuse the same payload and key only before that non-extendable cutoff, preserve the existing `sent` transport-acceptance state while storing `provider_accepted` classification plus the provider message ID, and route unresolved outcomes at or beyond the cutoff to manual reconciliation without blind resend.
- [ ] 6.6 Map Resend `invalid_idempotency_key`, `invalid_idempotent_request`, `concurrent_idempotent_requests`, `rate_limit_exceeded`, `daily_quota_exceeded`, `monthly_quota_exceeded`, and documented validation, authentication, domain, and policy types into stable outcomes; handle unknown types, non-JSON responses, network failures, and server failures conservatively and with bounded retries.
- [ ] 6.7 Run Resend dispatch only from a bounded jobs execution and prove Queue messages, deployment records, and logs never contain the API key or decrypted email body.
- [ ] 6.8 Add one shared email transport contract suite and run it against enterprise SMTP and Resend test Adapters, including configuration change before first claim, duplicate request, changed-payload idempotency conflict, crash before the side-effect boundary, crash or lease loss during submission, post-submission response loss, late provider completion, provider acceptance versus delivery, quota, outage, same-namespace credential rotation, and attempted or indeterminate delivery during a provider-namespace, relay, sender, endpoint, or Adapter change. Prove SMTP retries only after known pre-submission failure; prove a quiescent Resend network/5xx-indeterminate call replays only the byte-equivalent request in the frozen namespace with the same retained key; prove concurrent replay classification, near-cutoff clock skew, restart/non-extension, and cutoff expiry to manual reconciliation; and refuse automatic migration unless the rotation contract attests the same namespace and sender/domain scope.
- [ ] 6.9 Add explicitly authorized deep-verification paths for one SMTP or Resend test delivery while keeping ordinary `doctor` and core verification non-sending; require fresh operator/dashboard evidence for the declared provider namespace, verified domain, disabled authentication-mail tracking, team-shared rate posture, bounce/spam health, account suspension, and same-team/domain key rotation facts that a sending key cannot observe.
- [ ] 6.10 Keep Resend final delivery webhooks out of the first release and document that adding delivered, bounced, or complaint state requires raw-body signature verification, event deduplication, and out-of-order processing.
- [ ] 6.11 Document Resend-side retention separately from ShareSlices local payload deletion and do not present disabled provider content storage as a free-plan capability.
- [ ] 6.12 Add a non-public one-shot operator maintenance entrypoint that invokes the shared account application responsibility over a checked direct PostgreSQL connection and may have only a thin launcher under `deploy/automation/`; keep it outside the seven target lifecycle operations so it consumes no deployment plan or operation lease and changes no release or target state. Require a versioned canonical short-lived single-use signed envelope from an operator-controlled account-maintenance authority; configure only public verification material plus issuer/installation audience; bind subject, installation, action, delivery/revision, frozen transport-snapshot revision, attempt ID/fence, namespace, sender, local Message-ID, optional provider ID, payload digest, finite-provider safe-replay cutoff when present, evidence digest, nonce, and times; and reject invalid scope, correlation, expiry, signature, digest, or replay. Accept an initial resolution only for a manual-reconciliation delivery after its provider deadline/safety margin with no active lease and proven attempt quiescence and, when applicable, at or after its frozen cutoff. In one transaction uniquely claim the nonce, lock delivery plus authentication material, validate all identities and cutoff, and write the resolution/audit while serializing against dispatcher claims, code verification, and reset-grant issuance. Map acceptance to `sent`/`provider_accepted`, rejection to `failed`/`provider_rejected`, and unresolved closure to `failed`/`acceptance_unresolved`; never send email or expire, extend, invalidate, or recreate an existing code or reset grant. Permit a fresh-authorized repeat against a resolved delivery only when expected current revision, decision, evidence digest, frozen identities, and attempt fence exactly match; consume its new nonce and optionally add an invocation audit without changing delivery/authentication material or duplicating the resolution audit. Add active/expired-lease and late-completion refusal, pre-cutoff refusal, wrong-installation/team/message/payload/attempt/cutoff evidence, concurrent same-nonce, exact-match and mismatch fresh-nonce repeats, code/grant race, state-mapping, redaction, lifecycle-boundary, and later-new-delivery-with-code-reuse tests.

## 7. Resident and Bounded Background Processing

- [x] 7.1 Add a Runner interface over all enabled Rust processing lanes with shared claim, heartbeat, fence, retry, outcome, and storage dependencies.
- [x] 7.2 Implement and regression-test resident mode for Kubernetes without changing current authoritative job and terminal-result behavior.
- [x] 7.3 Implement bounded drain mode with maximum claims, idle observation, wall deadline, lane selection, and an explicit remaining-work result.
- [x] 7.4 Add graceful termination that stops new claims, bounds in-flight completion, and prevents a late unfenced result from committing.
- [ ] 7.5 Add a checked database dispatch or outbox record that is committed with new asynchronous work before a Cloudflare wake-up is published.
- [x] 7.6 Define a bounded non-sensitive Queue wake schema and producer that never includes authoritative state, credentials, raw Artifact bytes, or email payloads.
- [ ] 7.7 Implement the Queue consumer and Container controller with a stable bounded slot pool so each wake rereads PostgreSQL, starts or nudges one Durable Object-backed Container, records controller handoff separately from job completion, and never creates one Container identity per job.
- [ ] 7.8 Implement scheduled dispatch recovery, expired-lease recovery, dead-letter visibility, and a PostgreSQL-backed scheduled-execution gate whose closed state makes late or duplicate Cron invocations fenced no-ops.
- [ ] 7.9 Add duplicate, delayed, reordered, stale, lost, and concurrent wake tests proving one fenced authoritative outcome.
- [ ] 7.10 Add Container replacement, temporary-disk loss, wake-storm, deadline, and termination tests proving all recoverable state remains in PostgreSQL and private storage.
- [ ] 7.11 Preserve the thumbnail renderer revision, Chromium/font bundle, fixed viewport, request blocking, 10-second readiness deadline, split capture/output capabilities, and fenced immutable output through Kubernetes resident mode and the Cloudflare secretless Container plus execution broker.
- [ ] 7.12 Keep internal capture and the execution broker private on both targets and test that no public ingress or Worker route reaches either surface even with a valid-looking capture path or grant.
- [ ] 7.13 Set and test explicit Container `sleepAfter`, idle `stop()`, `SIGTERM` draining, forced-stop deadline behavior, ephemeral-disk recovery, remaining-work wake-up, and `ssh.enabled = false` with no authorized keys or SSH listener.

## 8. Immutable Release Construction and CI

- [ ] 8.1 Build Kubernetes API, Web, content/maintenance, and Rust/Chromium images once per source revision and publish immutable digests.
- [ ] 8.2 Produce an environment-neutral Web build and a canonical Static Assets manifest digest reusable by both targets.
- [ ] 8.3 Build immutable App, content, and jobs Worker bundles from the shared runtime entrypoints and record their compatibility date and bundle digests.
- [ ] 8.4 Build and retain separate trusted Rust processing and secretless Rust/Chromium Cloudflare images; always record content digests, use provider-proven digest references or qualified never-reused release tags, and keep every rollback-window image available.
- [x] 8.5 Generate the release manifest from source, artifact, migration, route/cache, verification, schema, and N/N-1 evidence.
- [x] 8.6 Reject release publication when a target artifact is missing, mutable, unverifiable, or incompatible with the declared schema and adjacent runtime version.
- [x] 8.7 Define the release-store and OCI-registry prerequisite contracts, including immutable object naming, separate build-push and deploy-pull credentials, Kubernetes image-pull references, `linux/amd64` qualification where required, digest/provenance verification, rollback-window retention, and garbage-collection refusal; publish the release manifest and target artifacts through those Adapters.
- [ ] 8.8 Add thin application release and target deployment workflows that call repository `mise` tasks and contain no duplicate deployment policy.
- [ ] 8.9 Add CI tests proving identical inputs produce equivalent release manifests and no production render resolves a mutable tag.

## 9. Kubernetes Target Composition

- [x] 9.1 Replace environment-copy overlays with one Kustomize base and composable workload, ingress, Gallery-isolation, sizing, and optional-CDN components.
- [x] 9.2 Remove fixed ClusterIPs and hard-coded internal addresses and use generated names plus Kubernetes Service discovery throughout.
- [x] 9.3 Remove the deployable placeholder Secret and render only references to pre-created or externally synchronized role-scoped Secrets.
- [x] 9.4 Replace the API init-container migration with one release-scoped migration Job using the immutable migration artifact and checksum checks.
- [x] 9.5 Render separate API HTTP and maintenance Deployments so API scaling does not multiply reconciliation or SMTP dispatch loops.
- [x] 9.6 Render Web and content-only Deployments and Services with the content runtime isolated from the trusted API application graph.
- [x] 9.7 Render resident Rust processing workloads that cover every enabled lane and expose correct health and graceful-termination behavior.
- [x] 9.8 Generate complete ingress routing from the source-linked route projection, including Viewer, Gallery trusted routes, health/readiness, Preview, and forbidden internal paths.
- [x] 9.9 Configure probes only against endpoints implemented by each role and add validation that rejects a missing or mismatched probe path.
- [x] 9.10 Add explicit resources, non-root identities, read-only roots with declared writable volumes, capability drops, no privilege escalation, seccomp, and service-account-token restrictions.
- [x] 9.11 Add default-deny NetworkPolicies and explicit API, maintenance, Worker, content, DNS, PostgreSQL, storage, SMTP, ingress, and internal-capture traffic; require each dynamic external dependency to use stable CIDRs, an operator egress gateway/proxy, or a qualified CNI FQDN extension instead of a broad Internet CIDR plus port.
- [x] 9.12 Generate TLS, ingress or Gateway class, replica, rollout, disruption, and scheduling configuration from deployment input.
- [x] 9.13 Add deterministic render, schema, per-role environment, Secret, route, security-context, and NetworkPolicy contract tests.

## 10. Kubernetes Lifecycle, GitOps, and Optional CDN

- [ ] 10.1 Implement read-only Kubernetes `doctor` checks for explicit context, namespace, supported APIs, permissions, declared CNI and egress mechanism, current conformance evidence, ingress/TLS, dependency metadata, Secrets, SMTP configuration, images, release-store access, and connection budget; never report API or CNI presence as enforcement proof.
- [x] 10.2 Implement deterministic phased `render` and `kubectl apply --server-side --dry-run=server` planning with an explicit field manager, conflict reporting, and no persistence for prerequisites, migration, workloads, ingress, and optional edge.
- [ ] 10.3 Implement direct `apply` ordering for policies and configuration, isolated pre-traffic allowed/denied network probes with cleanup, one-shot migration, private workloads, trusted runtimes, ingress, verification, retirement, and release recording.
- [x] 10.4 Implement Kubernetes `status` from observed image IDs, generations, probes, migration head, configuration and route digests, release marker, and optional capabilities.
- [x] 10.5 Implement Kubernetes `rollback` from the prior immutable runtime and configuration bundle against the current schema and verify that it never reruns the prior migration or applies a down migration.
- [x] 10.6 Add render-only GitOps handoff with separate ordered phase bundles, predecessor evidence, `external_reconciler_required` apply/rollback outcomes, observed phase-block status, and reconciliation-owner validation; rollback emits current-schema compatibility evidence plus prior runtime/configuration bundles, not the prior migration Job, and never writes a Git repository or assumes generic controller ordering.
- [x] 10.7 Add `delivery.mode: direct` and `delivery.mode: external-cdn` without deploying any Cloudflare-target application runtime.
- [x] 10.8 Generate and validate origin-access, trusted-proxy, route, and cache requirements for an external CDN without provisioning its provider account.
- [ ] 10.9 Verify direct ingress and external edge against the same status, header, Cookie, CORS, credential, route, and cache scenarios and fail on divergence.
- [ ] 10.10 Verify real Gallery registrable-site, Cookie, route, content-runtime, and network evidence and keep Gallery fail-closed when any dimension is unproven.

## 11. Cloudflare Target Resources and Runtimes

- [ ] 11.1 Add pinned Cloudflare Terraform IaC for ShareSlices-owned private R2, Queues and dead-letter queue, Hyperdrive, routes, and custom domains inside existing accounts and zones; add Cron triggers, Queue consumers, and queue-level `delivery_paused` only if task 1.10 selects Terraform as their single qualified owner. Render a private-prerequisite phase with no Worker custom-domain attachment or serving address and a separate trigger/ingress activation phase.
- [ ] 11.2 Document and test the IaC state backend, encryption, access control, import, drift, and recovery requirements as Secret-bearing infrastructure state without committing state or sensitive variables.
- [ ] 11.3 Define and enforce a field-level ownership matrix for R2 public access, long-lived product Queues/consumers, release-only verifier Worker/Queue/consumer/evidence resources, queue delivery pause/resume, Cron, routes/domains, `preview_urls`, `workers_dev`, ordinary and Secret bindings, Durable Object migrations, Container image and rollout, the authoritative PostgreSQL operation lease/fence/journal, and the mirrored R2 deployment-state object so Terraform, Wrangler, the operator Secret source, and the Deployment Module never manage the same field. A Worker-coupled field may be split away from Wrangler only after repeated Terraform apply plus every deploy path proves preservation and empty drift; otherwise select one owner and block activation.
- [ ] 11.4 Add pinned Wrangler inputs for staged App/Content versions, trigger-isolated immediate Jobs deployment, and both release Container images; consume only validated IaC outputs and the qualified ownership matrix, use version-upload Secrets for App/Content and preserved or ephemeral deploy-time Secrets for Jobs, and omit only those routes, domains, Cron, Queue-consumer, or other fields whose external ownership passed the task 1.10 source-of-truth gate.
- [ ] 11.5 Implement App Worker host-aware dynamic routing before Static Assets fallback and preserve bounded streaming, Cookies, headers, errors, and forbidden `/internal/*` behavior.
- [ ] 11.6 Bind the App Worker to cache-disabled Hyperdrive, private R2, runtime Web configuration, and only the Secrets required by the trusted HTTP role.
- [ ] 11.7 Deploy the content-only Worker on a separate registrable site with only cache-disabled database lookup, private manifest-backed R2 reads, policy headers, health, and telemetry.
- [ ] 11.8 Add build-time and deployed dependency-graph checks that reject management, Better Auth, credentialed CORS, email, Queue, or mutation authority in the content Worker.
- [ ] 11.9 Deploy the Jobs Worker with no public route and only Queue, schedule, Resend, database, dispatch-recovery, Container-control, and private thumbnail-broker capabilities; set production `workers_dev = false` and `preview_urls = false` for every Worker and verify both through `doctor`.
- [ ] 11.10 Configure Queue consumers, queue-level pause/resume compensation, retry and dead-letter behavior, the scheduled-execution gate, and UTC scheduled recovery. For Cron addition, change, and removal, reread the expected control-plane state and enforce the full maximum propagation interval from the pinned platform baseline while the scheduled gate remains closed; never claim the provider exposes global propagation completion.
- [ ] 11.11 Configure stable Container slot addressing, selected predefined or qualified custom instance resources, `max_instances`, explicit `sleepAfter`, claim and wall-time limits, concurrency bounds, idle `stop()`, operator safety caps, and immutable embedded image build/release/contract identities mapped by the release manifest to the image content digest and qualified provider reference.
- [ ] 11.12 Export the required `ContainerProxy`, keep thumbnails Internet-disabled on an internal HTTP broker allowlist, enable trusted-Runner Internet only for exact direct-PostgreSQL hosts, configure explicit PostgreSQL hostname/certificate verification and least-privilege credentials, configure the injected CA only when HTTPS interception is used, and disable SSH for every production Container. Report `allowedHosts` only as whole-Container host filtering, never port-level, per-process, stable-egress-IP, or Chromium isolation.
- [ ] 11.13 Expose paid prerequisites, provider-observed/release-static/operator-evidenced limit sources, unknown required facts, configured maxima, quota headroom when observable, Worker-first route consumption and 429-without-static-fallback risk, and cost-risk warnings without promising an exact bill.
- [ ] 11.14 Add the direct one-shot migration execution path from the deployment control plane with advisory locking and immutable checksum validation.
- [ ] 11.15 After the authoritative PostgreSQL lease exists, update the Secret-free Cloudflare deployment-record mirror only after validating the current PostgreSQL fence and comparing the encoded mirror fence. Use ETag match for an existing object and `If-None-Match: *` or a qualified wildcard equivalent for first create through a pinned Workers R2 binding or S3-compatible path. Tag every observed App, Content, and Jobs deployment with the common release ID. Treat precondition failure, lease loss, or an ambiguous response as stale or indeterminate and reconcile from the PostgreSQL journal plus current object without old-fence retry; the R2 object never grants or extends a lease.
- [ ] 11.16 Add local/emulated route and binding tests plus staging tests for platform behavior that cannot be represented faithfully by emulation.
- [ ] 11.17 Add an explicit Edge/CDN configuration with default `web-assets-only` and optional `web-and-public-viewer-bytes` modes; generate Static Assets `_headers` for immutable hashed assets and revalidated HTML/bootstrap, attach headers directly to Worker responses, and route all API, authentication, management, Upload, Preview, stable Viewer, and authorized content families through Worker logic before Static Assets.
- [ ] 11.18 Implement the optional Viewer byte cache behind current Publication authorization with complete Version-content-path-representation identities, private R2 reads, a separate cacheable internal full-body `200` response, independently constructed outward `no-store` responses, Cache API size bounds, Range/206 bypass to R2, cache tags for hygiene, and no public R2 or internal-cache address.
- [ ] 11.19 Measure Cache API hit ratio, data-center locality, R2-read reduction, oversized-response rejection, and cost on representative traffic before recommending the optional Viewer mode; do not infer global tiered-cache behavior from `cache.put()` or use `CF-Cache-Status` as sole evidence.
- [ ] 11.20 After the shared runtime seams and Adapters exist, rerun the complete production qualification suite in a disposable account for Worker contract parity, App/Content versioned Secrets, Jobs Secret preservation/update/delete compensation, private R2, direct and Hyperdrive database paths, Container egress and split-capability isolation, Resend, the route-free verifier harness and fetch-based version overrides, staged versions, trigger-isolated Jobs deployment, rollback retention, and ownership drift before enabling Cloudflare activation.

## 12. Cloudflare Lifecycle and Rollback

- [ ] 12.1 Implement read-only Cloudflare `doctor` checks for Workers Paid entitlement, pinned toolchain/schema baseline, zones, distinct registrable sites, DNS/TLS, configured Upload and asset sizes, declared Worker and trusted-Container PostgreSQL paths, Hyperdrive modes, private R2, Queue, per-role Worker CPU limits, bounded Container settings, Resend references, versioned Secret revisions, release-store access, and current qualification evidence; classify limits as provider-observed, release-static, or operator-evidenced and block required unknown or stale facts rather than claiming all quota headroom is live-readable.
- [ ] 12.2 Implement Secret-free Cloudflare `render` and read-only `plan` with IaC, version, route, migration, compatibility, and destructive-action phases.
- [ ] 12.3 Before any first-install provider mutation, prove from the authorized plan that no active or rollback predecessor exists and that every proposed long-lived field is compatible with that absence. Make the qualified Jobs Container image available and verify it before bootstrapping every final script name through the pinned immediate-deployment interface with no custom domain, public route, or active trigger; the initial Jobs bootstrap selects that image and declares all stable Container Durable Object classes. Treat that bootstrap bundle only as stable identity setup, not as the release Jobs bundle. Before adding any App or Content candidate to a current deployment, prove it already enforces the complete production authentication, authorization, route, Cookie, cache, security-header, logging, and Secret contract, contains no preview-only authority, and treats its version ID as non-secret. Only after each script exists and that security gate passes, stage App and Content Worker/Static Assets versions at zero percent and record identities verified by the live provider API; route later Durable Object lifecycle changes to a separately authorized maintenance procedure.
- [ ] 12.4 Before every release Jobs deployment after bootstrap, prove the active and rollback releases compatible with all long-lived fields when they exist and reuse the 12.3 absence proof on first installation; make the qualified image available. On upgrade close the scheduled-execution gate, pause Queue delivery, detach Cron, reread its control-plane absence, wait the full maximum propagation interval from the pinned baseline, drain or fence in-flight work, and checkpoint the isolated state. On first installation prove the bootstrap has no Queue delivery, Cron, public route, `workers.dev`, or preview URL and require migration to create the gate closed before trigger attachment. Deploy the distinct release Jobs bundle immediately through the qualified `exports` path with target Secret handling and restore the isolated state on interruption.
- [ ] 12.5 Implement the explicitly authorized pre-traffic release-only verifier harness: a positively owned route-free Worker with `workers_dev` and preview URLs disabled, an isolated temporary Queue and initially paused consumer, fetch-based Service Bindings to App, Content, and Jobs, and a private release/fence evidence prefix. Reconfirm that staged App/Content candidates enforce the complete production security contract; test that external override requests through an existing route cannot bypass it, and do not treat the version ID as a Secret. Publish one nonce-bound message containing expected App/Content versions, Jobs deployment/bundle/configuration identity, and Container build/provider identities; resume only that Queue for the bounded idempotent probe; verify actual App/Content IDs and exact Jobs identity; and exercise every production-capable stable trusted-processing and thumbnail slot, requiring its embedded build/release/contract, class, slot, provider-instance, release, fence, and nonce evidence to match with no previous-image instance selectable. Before cleanup atomically record evidence, mark the nonce terminal, and advance its sub-fence; pause/detach the consumer without claiming it drained in-flight work, drain or fence nonce-owned active invocation leases through the bounded external-write window, then run final inventory and idempotent cleanup. Retain the terminal tombstone through the observed maximum of Queue retention, send/retry delay and schedule, active-invocation lease, interrupted recovery, and cross-storage side-effect intervals plus safety margin without calling that retention an orphan or blocking activation. Unconfirmed nonce state, quiescence, or cleanup leaves an isolated reported orphan and cannot activate product ingress or triggers.
- [ ] 12.6 Complete activation after all pre-migration probes: observe Jobs and rolling Containers, apply the one-shot migration, and use the release-only harness to reverify exact App/Content versions, authorized Jobs deployment/bundle/configuration, functional database/broker behavior, and the actual expected build in every production-capable stable Container slot against the observed migration head. Require nonce marking, bounded quiescence, and final cleanup before product activation; normal retained tombstones do not block it. On first installation promote and reverify Content and App while ingress is absent, replace each current deployment with candidate-only membership, observe that exact state, and prove an override cannot select either bootstrap version before safely detaching the harness; attach Content ingress and the App custom domain/public routes last, and run black-box verification while Cron is absent, Queue delivery is paused, and the gate is closed. Only then attach Cron, reread its control-plane presence, wait the full pinned maximum interval, resume Queue delivery, and open the gate last. On upgrade keep existing entry resources attached while shifting Content first and App/Static Assets last, black-box verify with triggers isolated, then restore Cron through the same safety check, Queue delivery, and the gate. Compensate first-install failure by removing positively owned new ingress/Cron, restoring bootstrap-only no-ingress current deployments when a pre-traffic candidate failed, pausing Queue delivery, and keeping the gate closed; retire eligible resources and record the release only after all phases pass.
- [ ] 12.7 Implement Cloudflare `status` from actual versions, deployments, routes, bindings, scheduled-gate and Queue-delivery state, per-stable-slot runtime image build identity and rollout state, migration head, Cron control-plane state plus elapsed or remaining safety interval, verifier terminal nonce/tombstone/quiescence/cleanup state, release record, capabilities, and drift; distinguish expected retained tombstones from blocking orphans and never report global Cron propagation as observed.
- [ ] 12.8 Implement compatible Cloudflare rollback by first proving the exact App/Content versions remain provider-addressable within the current 100-published-version retention window after bootstrap/verifier churn and every required binding resource, retained Jobs bundle, and Container image still exists. Then reactivate retained App and Content versions with their Static Assets, bindings, and version-scoped Secrets; close the scheduled-execution gate, pause Queue delivery, detach Cron, reread control-plane absence, wait the full pinned maximum propagation interval, redeploy the retained compatible Jobs bundle, and verify the retained embedded build identity on every production-capable stable Container slot with no forward-image instance selectable. After verification, restore Cron through the same control-plane and safety-window check, then Queue delivery and the gate in order. Leave long-lived entry resources and prerequisites current under their qualified owner, prove N/N-1 compatibility, refuse rollback across a Durable Object lifecycle change, and refuse when any required identity or resource is unavailable; a local release bundle cannot restore an evicted provider version or deleted image.
- [ ] 12.9 Add interrupted apply, failed migration, wrong Jobs deployment/bundle/configuration evidence, one or more stable slots returning an old/wrong embedded image identity, a previous-image instance remaining selectable, failed staged or post-migration harness probe, duplicate already in flight when nonce becomes terminal, a late Queue retry, an R2 write crossing the pre-check before terminal state, unconfirmed active-invocation quiescence, verifier cleanup orphan versus normal retained tombstone, failed candidate activation reverification, first-install candidate-only membership and bootstrap-override rejection, first-install public-verification compensation, partial upgrade traffic switch, Queue pause/resume compensation, Cron safety-window recovery, scheduled-gate recovery, failed verification, resume, and separate Worker/Container rollback tests. Every upgrade failure path SHALL replace and observe previous-only current deployments so the failed candidate is no longer override-selectable; failed or indeterminate removal SHALL report a degraded blocked release, keep triggers isolated, and state that the candidate remains externally selectable.
- [ ] 12.10 Verify an idle installation leaves no resident processing Container while scheduled recovery and future Queue wakes remain functional.
- [ ] 12.11 Qualify and implement rollback-aware Jobs Secret-binding retirement as a separately authorized scheduled-gate-closed, Queue-paused, Cron-detached maintenance operation; refuse while a retained bundle needs the name or the pinned `exports` deletion path is unproven. Because the deletion interface immediately deploys a Worker version, prove code, ordinary configuration, `exports`, Container image, and retained bindings are unchanged, then verify Jobs/Container convergence before restoring triggers. If deletion succeeds but post-deletion checks fail or remain indeterminate, keep triggers isolated and re-add the binding from the operator Secret source with the exact retained bundle or require an authorized forward-fix.

## 13. Canonical Local Compose Development and Test Topology

- [x] 13.1 Refuse to begin until 2.4 records that `consolidate-local-development-stack` is archived. Then move the base and Gallery-local Compose inputs under `deploy/compose/`; define separate fixed ordered `-f` lists for developer and test topologies, and make every call pass its list, repository-root `--project-directory`, and explicit `-p shareslices` or `-p shareslices-test`. Retain root `.env` only for the developer topology. Launch every test Docker/Compose child from a newly constructed hermetic allowlist containing only minimal process inputs, an isolated Docker client configuration, the frozen endpoint/TLS inputs, and the checked non-sensitive `--env-file` fixture; inherit no caller environment, registry credential, Docker/Compose control, application value, CI metadata, or agent variable. Preserve every build context, bind mount, environment-file reference, network, existing developer project, and named-volume identity.
- [x] 13.2 Move local lifecycle and API/Web-test composition policy under `deploy/automation/local-compose/`; keep `.mise.toml` as the public entrypoint, leave only policy-free compatibility wrappers under `tools/` if required, and update every command, validator, test assertion, and durable document that references the root Compose files or their resolved paths.
- [x] 13.3 Preserve exactly `mise run dev`, `dev-status`, `dev-logs`, and `dev-down` as the supported interactive lifecycle. Resolve a selected context once into a normalized endpoint/TLS/client-configuration snapshot, use that explicit snapshot rather than the mutable alias on every command, and reject conflicting ambient connection controls. Make every mutation run only through the controller while it continuously holds both endpoint/project and observed-Engine-ID/project host-global locks from first mutation through `finally`; bracket build, create, start, `up`, `run`, `exec`, recreation, `down`, orphan removal, and cleanup with before/after Engine-ID checks. On identity change, perform no action under the stale Engine lock and acquire the replacement Engine/project lock for positive-ownership reconciliation or fail closed. Keep status/log paths read-only but endpoint/project-pinned and report an Engine change as indeterminate, reject `compose` in the production target schema, and do not add a production deploy operation or second human-facing local launcher.
- [x] 13.4 Compose PostgreSQL, private MinIO and its one-shot initialization, Mailpit, one-shot migration, API HTTP, maintenance/authentication-email, content-only HTTP, resident processing, and Web/Caddy as separate real roles; keep core services outside optional profiles, use the isolated test project or `run --rm` for diagnostics/tests, carry any long-lived profile set through the full lifecycle, and prohibit local-only authority recombination.
- [x] 13.5 Record and enforce a tested Docker Compose feature baseline for bounded `up --wait`/`--wait-timeout`, long-form `service_healthy` and `service_completed_successfully`, and machine-readable `ps`; fail before mutation when a required feature is absent, retain application retries and capability-based health checks, and run post-start host probes for canonical Web, API, content, Mailpit, and SMTP reachability.
- [x] 13.6 Keep local credentials, the Mailpit SMTP URL, the Turnstile test key, and every published port explicitly non-production and caller-loopback-only. Preserve canonical developer ports, but let the Engine dynamically assign every test host port. In one locked lifecycle, create only the ingress/proxy and dependency endpoint layer that has no public-URL dependency or business side effect, discover and freeze its machine-readable bindings, inject the resulting test endpoints into the remaining role configuration, and then start application roles without recreating or renumbering the frozen layer. Reject external test networks, volumes, configs, or Secrets, explicit resource names outside project scope, `container_name`, and any other shared-resource escape before mutation. If `local-development-stack` separately implements its permitted explicit non-production remote-access profile, prohibit that profile from automated tests or production-qualification evidence and require a single controller host or daemon-side/distributed lease because local OS locks cannot coordinate multiple clients; do not create that profile in this change. Prove local configuration cannot render into a production target bundle and `dev-down` preserves canonical named volumes.
- [x] 13.7 Route API integration and Web E2E through the fixed `shareslices-test` project and dynamically discovered, test-only Web/API/content/Mailpit/SMTP/database/object-store endpoints; require trusted and Untrusted-content addresses to pass the distinct-browser-site validator and reject same-host different-port or developer-default endpoints. Always reject remote daemons for tests. Acquire an endpoint/project lock before connection, derive daemon identity from the Docker Engine server ID, acquire the matching Engine/project lock to coalesce aliases, and hold both through failure-safe cleanup so a second run waits or fails closed. Require the same repository/topology/endpoint/Engine/project ownership marker, bracket every mutation and cleanup with Engine-ID checks, report cleanup failure without masking the original failure, and remove only that project's orphans and volumes without pre-emptive or global cleanup.
- [x] 13.8 Run only read-only readiness, route, and response-header checks against the canonical developer Compose topology. Route every authorization, credential mutation, Viewer-isolation, authentication-email lifecycle, stateful smoke flow, and other writing or sending verification through the locked `shareslices-test` project; emit stable `not_applicable` evidence for Kubernetes CNI, external CDN, Workers Static Assets/Cache API, R2, Hyperdrive, Queues, Containers, and Resend checks.
- [x] 13.9 Add safe `docker compose config --quiet` validation against real developer inputs without returning the resolved model. Permit a full model or `config --environment` only inside the hermetic non-sensitive test environment and do not persist it by default; permit a required model hash or snapshot only after proving fixture-only input, never after-the-fact redaction of real inputs. Test each topology's ordered files, explicit project directory/name, immutable endpoint/TLS use, endpoint-plus-Engine lock lifetime and lock ordering, context update, Engine reset before/during/after a mutation, replacement-lock recovery, remote-daemon rejection or conditional developer-profile handling, total caller-environment exclusion, no developer `.env` reuse in tests, and canaries for a declared shell override, an unrelated Secret-like value, Docker/Compose controls, and expected fixture values. Also test Compose feature baseline, path resolution, developer volume retention, two-phase dynamic loopback endpoint allocation, second-phase non-recreation, port collision handling, external/shared-resource rejection, fixed-test-project ownership and crash recovery, E2E distinct-site and developer-default rejection, readiness failure, exact cleanup scope, cleanup-error preservation, and production-target rejection. Crash recovery may adopt a stale project only while holding both host-global locks when the repository identity and topology digest match and every resource is positively project-owned; unknown or mismatched markers must fail closed with exact recovery diagnostics.
- [x] 13.10 Exercise a cold `mise run dev`, `dev-status`, repeated `dev`, and data-preserving `dev-down`; while the developer stack remains a healthy untouched control, run `mise run api-test` and relevant `mise run web-e2e` through the isolated test controller and `shareslices-test` endpoints, prove developer containers and volumes remain unchanged, and retain test and cleanup diagnostics separately from the ordinary `mise run check` gate.

## 14. Shared Verification and Failure Drills

- [ ] 14.1 Implement the shared read-only core verifier over configured Web, API, Viewer, content, origin, and edge addresses with stable machine-readable evidence, explicit `core` level, and an explicit local-Compose applicability projection; it creates no state, starts no work, changes no authorization, and sends no mail.
- [ ] 14.2 Verify route ownership and prove public ingress cannot reach management through Viewer/content hosts or reach internal capture, maintenance, migration, and control routes.
- [ ] 14.3 In core mode, verify statuses, redirects, request IDs, Cookies, CORS, security headers, `no-store`, content-hashed static eligibility, dynamic bypass, and private-storage non-exposure from read-only response and observed-state evidence. Put R2/origin-read instrumentation that mutates counters, authorization transitions, and optional Viewer-cache population under explicitly authorized pre-traffic or deep rows; use `CF-Cache-Status` only as corroboration.
- [ ] 14.4 In explicitly authorized isolated deep verification, run the same Viewer route and current-state cache tests through direct Kubernetes ingress, Kubernetes external CDN, and both Cloudflare edge modes, including full-body internal hit, Range/206 bypass, and cache hit followed by Unpublish, expiry, replacement, and restriction; clean only positively owned test state.
- [ ] 14.5 Verify Gallery independently from core health and keep it disabled unless actual registrable-site, credential, governance, content, and network gates pass.
- [ ] 14.6 Implement explicitly authorized deep smoke for Upload, processing, Preview, Publish, Viewer, Unpublish, Gallery eligibility or fail-closed behavior, and cleanup of only test-owned data.
- [ ] 14.7 Add deep SMTP delivery and Resend provider-acceptance probes with test recipients, redacted evidence, durable delivery and idempotency-window checks, disabled-tracking evidence, and no dependency on API request readiness.
- [ ] 14.8 Add synthetic duplicate-wake, lost-wake, Container-termination, stale-fence, and follow-on-work checks only to explicitly authorized pre-traffic verification or isolated release drills, never default core verification.
- [ ] 14.9 Add drift drills for Kubernetes resources, Cloudflare versions/routes/bindings, configuration digests, and deployment records.
- [ ] 14.10 Add failure drills for migration failure, dependency outage, incorrect CDN caching, Resend rate limiting, SMTP outage, unsafe Gallery topology, and incompatible rollback.
- [ ] 14.11 Prove ordinary apply refuses destructive prerequisite actions, retires only positively inventoried release-owned resources after traffic and schedule removal, and reports the separately reviewed recovery or migration procedure required for every other deletion.
- [ ] 14.12 Report core, email, processing, thumbnail, CDN, and Gallery readiness separately and never mark a release or capability verified on failed or indeterminate evidence.
- [ ] 14.13 Emit and verify unified deployment-operation lease/fence, phase, migration head, job backlog/lease, Queue/DLQ, trigger delay, Container startup/runtime, database connection, R2 usage, SMTP, Kubernetes resource/probe, provider-limit headroom, and cost-risk telemetry with stable redacted attributes and alert thresholds; report Resend quota or account-health facts only from provider responses or fresh operator/dashboard evidence and otherwise emit a stable `unknown` classification.

## 15. Operator Documentation and Final Gates

- [ ] 15.1 Document the `deploy/` module, the non-production Compose boundary and canonical `mise` lifecycle, production lifecycle commands and JSON results, separately authorized one-shot maintenance commands, authenticated operator-principal and audit requirements, configuration schema, release bundle and store, Secret references/revisions, operation lease/fencing, release inventory/retirement, records, and supported ownership boundaries.
- [ ] 15.2 Add a Kubernetes runbook for existing-cluster prerequisites, enforceable egress, enterprise SMTP, direct apply, render-only GitOps handoff and external promotion, status, qualification, verification, recoverability evidence, and rollback.
- [ ] 15.3 Add a Cloudflare runbook for Workers Paid and limit-evidence prerequisites, bounded per-role CPU and other cost-driving settings, provider budget alerts or spend controls only when actually observed, explicit Edge/CDN modes and cache boundaries, separate registrable sites, Worker and trusted-Container PostgreSQL reachability, outbound interception, private R2, App/Content staged Secret rotation, Jobs trigger-isolated Secret rotation and rollback-aware binding retirement, Resend account/domain/key/tracking/retention, IaC state, release, status, and rollback. Include an emergency trigger/route shutdown procedure and a disposable-prototype teardown checklist that rereads provider inventory and records the owner and expiry of every retained private prerequisite.
- [ ] 15.4 Add an external-CDN runbook that explains provider-neutral configuration, trusted ingress, permitted static caching, dynamic bypass, origin comparison, and why the target remains Kubernetes.
- [ ] 15.5 Add recovery procedures for lease loss, interrupted release, failed or partially applied migration, drift, orphan retirement, Secret rotation failure, Queue backlog, Container failure, SMTP/Resend degradation and manual delivery reconciliation, incompatible rollback, lost deployment state, and database/object restore-point mismatch.
- [ ] 15.6 Publish the operationally breaking migration guide from the old Kubernetes examples, including fixed addresses, placeholder Secrets, image tags, and init-container migration removal.
- [ ] 15.7 Update `PRODUCT.md`, `CONTEXT.md`, module architecture, local-development guidance, README test guidance, and operations docs before archival; correct the Gallery guide to describe the canonical isolated-content topology and fail-closed eligibility rather than an opt-in Compose profile, record the tested Compose feature baseline, and use `/opsx:archive` to apply completed delta specs to the implemented OpenSpec store rather than editing living specs manually.
- [ ] 15.8 Confirm public OpenAPI payloads and end-user CLI behavior remain unchanged, keep the new thumbnail-broker and route-free Jobs release-verification contracts explicitly private and unreachable in the checked OpenAPI ownership model, and document and specify any other discovered contract change before implementation continues.
- [ ] 15.9 Run focused API, Web, Worker, Compose-controller, deployment-schema, render, route, cache, security, email, and target-Adapter tests while iterating.
- [ ] 15.10 Run `openspec validate support-kubernetes-and-cloudflare-deployment-targets --type change --strict --no-interactive`, documentation checks, `git diff --check`, and the authoritative `mise run check` gate.
- [ ] 15.11 Run deep verification on representative Kubernetes direct, Kubernetes external-CDN, and Cloudflare staging installations and retain redacted release evidence.
- [ ] 15.12 Deprecate or remove the old unsupported Kubernetes example interface only after migration documentation and the Kubernetes direct-target acceptance gate pass; Cloudflare feasibility MUST NOT delay removal of an interface replaced solely by the supported Kubernetes path.
- [ ] 15.13 Refresh the official-platform evidence against live Docker Compose, Cloudflare, Kubernetes, and Resend manuals and generated provider schemas immediately before implementation acceptance; reconcile any changed interface or limit before marking a target or local lifecycle supported.
- [ ] 15.14 Define owners, encrypted locations, retention, evidence age, RPO/RTO, restore order, and the database/object/recovery-manifest consistency-marker format for PostgreSQL, S3/R2, Terraform state, release bundles, and deployment journals; create the marker at a documented cut and run an isolated restore drill that fails closed when markers do not match.
- [ ] 15.15 Do not mark the Cloudflare capability complete or archive this combined change until every implementation-blocking Cloudflare gate passes. If Kubernetes is released first after a Cloudflare gate fails, move the remaining Cloudflare specification and tasks into a follow-up change and validate both change boundaries before archival.
